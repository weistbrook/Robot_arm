# 先忽略所有文件和文件夹（作为基础规则）
*

# 取消忽略 train 文件夹（保留文件夹本身）
!train/

# 取消忽略 train 文件夹内的所有内容
!train/**/*

# 取消忽略 yolotxt 文件夹（保留文件夹本身）
!yolotxt/

# 取消忽略 yolotxt 文件夹内的所有内容
!yolotxt/**/*

# 保留 .gitignore 文件本身（否则会被第一条 * 规则忽略）
!.gitignore